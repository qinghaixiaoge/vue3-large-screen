import { createStore } from 'vuex'
import loginUser from './loginUser'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    loginUser
  }
})
